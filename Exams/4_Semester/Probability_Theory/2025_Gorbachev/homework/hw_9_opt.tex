\subsection{Домашние задачи на десятую неделю (необязательные).}
\begin{problem}
    Вычислите $\lim\limits_{n \ra +\infty} A_n$ для множеств $A_n = [0, n/(n + 1)]$.
\end{problem}
\begin{solution}
\end{solution}
\begin{problem}
    Пусть $B$ и $C$ -- подмножества $\Omega$, а
    \[
        A_n = \begin{cases}
                  B, & n = 2k, k \in \Z, \\
                  A, & n = 2k + 1, k \in \Z.
        \end{cases}
    \]
    Найти $\limsup A_n$ и $\liminf A_n$.
\end{problem}
\begin{solution}
\end{solution}
\begin{problem}
    Докажите, что множества $A_n \ra A$ тогда и только тогда, когда $I_{A_n} \ra I_A$ поточечно.
\end{problem}
\begin{solution}
\end{solution}
\begin{problem}
    Пусть $\{\xi_n\}_{n \in \N}$ -- независимые случайные величины.
    Докажите, что
    \[
        \P(\sup\limits_{n} \xi_n < +\infty) = 1 \Longleftrightarrow \exists M \in \R \sum\limits_{n} \P(\xi_n \geq M) < +\infty.
    \]
\end{problem}
\begin{solution}
\end{solution}
\begin{problem}
    Докажите, что следующие события -- остаточные:
    \begin{itemize}
        \item $A_1 = \{\limsup(\xi_n \in I_n)\}$, где $I_n$ -- борелевские множества на прямой.
        \item $A_2 = \{\limsup \xi_n < +\infty\}$.
        \item $A_3 = \{\limsup \dfrac{S_n}{2n \ln n} = 1\}, S_n = \sum\limits_{n = 1}^{+\infty}$.
        \item $A_4 = \{\dfrac{1}{n}\sum\limits_{k = 1}^{n} \xi_k \text{ -- сходится }\}$.
    \end{itemize}
    Докажите, что следующие события -- не остаточные:
    \begin{itemize}
        \item $A_5 = \{\xi_n = 0, n \geq 1\}$.
        \item $A_6 = \{\exists \lim\limits_{n \ra +\infty} (\xi_1 + \ldots + \xi_n) < c\}$.
    \end{itemize}
\end{problem}
\begin{solution}
\end{solution}